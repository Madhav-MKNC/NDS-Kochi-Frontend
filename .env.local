# Environment Variables Configuration

# Next.js application configuration
# The NEXT_PUBLIC_ prefix makes these variables available in the browser
# Only use NEXT_PUBLIC_ for values that are safe to expose to the client

# ===== API Configuration =====
# Backend API base URL - used for all API requests
# This should point to your backend server endpoint
NEXT_PUBLIC_API_URL=http://localhost:8000

# ===== Authentication Configuration =====
# JWT secret key for token verification (server-side only)
# Generate a secure random string for production
# JWT_SECRET=your-super-secure-jwt-secret-key-here

# Session timeout in milliseconds (optional)
# SESSION_TIMEOUT=3600000

# ===== Database Configuration =====
# Database connection string (server-side only)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Redis connection for session storage (optional)
# REDIS_URL=redis://localhost:6379

# ===== Email Configuration =====
# Email service provider settings (server-side only)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ===== File Upload Configuration =====
# Maximum file upload size in bytes
# MAX_FILE_SIZE=10485760

# File storage service (AWS S3, Cloudinary, etc.)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# ===== Third-party Integrations =====
# Google Analytics tracking ID (client-side)
# NEXT_PUBLIC_GA_TRACKING_ID=GA-XXXXXXXXX

# Sentry DSN for error tracking (client-side)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ===== Development Configuration =====
# Environment mode
NODE_ENV=development

# Application port (defaults to 3000)
# PORT=3000

# Enable debug logging
# DEBUG=true

# ===== Production Configuration =====
# Domain configuration for production
# NEXTAUTH_URL=https://yourdomain.com
# NEXTAUTH_SECRET=your-nextauth-secret

# ===== Notes =====
# 1. Never commit sensitive values like API keys or passwords to version control
# 2. Use different .env files for different environments (.env.local, .env.production)
# 3. Variables without NEXT_PUBLIC_ prefix are only available on the server side
# 4. Restart your development server after adding new environment variables
# 5. For production, set these variables in your hosting platform's environment settings